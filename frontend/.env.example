# URL da API backend. Ainda não é consumida pelo frontend (todos os dados são
# mockados em src/app/data.ts) — a variável já fica pronta para quando as
# páginas passarem a chamar o backend real.
VITE_API_URL=http://localhost:4000
